# legacy results to database

Process `results` dir, group ini-opt structures,
and prepare to write to mongodb

1. group ini-opt structures
2. extract properties (enthalpy and pressure etc, not geo-info)
